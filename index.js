/**
 * Multiply two numbers
 * 
 * @category Category
 * @param {number} a The first factor number.
 * @param {number} b The second factor number.
 * @returns {number} The product number.
 * @mermaid
 *   graph TD;
 *     A-->B;
 *     A-->C;
 *     B-->D;
 *     C-->D;
 */
function multiply(a, b){
  return a * b
}

/**
 * Dummy Class For testing perpose
 * @category Category
 * @class
 * @classdesc Work with numbers...
 */
class Myclass {
  /**
   * Multiply two numbers
   * 
   * @param {number} a The first factor number.
   * @param {number} b The second factor number.
   * @returns {number} The product number.
   */
  multiply(a, b){
    return a * b
  }
  /**
   * Add two numbers
   * 
   * @param {number} a The first factor number.
   * @param {number} b The second factor number.
   * @returns {number} The product number.
   */
 add(a, b){
    return a + b
  }
}